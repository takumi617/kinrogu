<% @users.each do |e| %>
    <%= e.uid %>
    <%= e.password_digest %>
    <%= button_to "削除", user_path(e), method: :delete, onclick: "return confirm('本当に削除しますか？')" %>
<br>
<% end %>
<%= link_to "新規追加", new_user_path %>